# Smoke test — auto-add al tablero Projects v2

Archivo descartable para verificar que el workflow `pr-project.yml` agrega el PR
al tablero #4 vía `PROJECT_TOKEN`. Este PR NO se mergea; se cierra tras la verificación.
